# canMOD4 / HT42B416 — bus-off без восстановления

Стенд: WB8.5 (`ABSSUO77`), ядро `6.8.0-wb158`. Модуль WBE2-I-CAN (чип Holtek **HT42B416**, fw v1.02)
в слоте mod4 → интерфейс `canMOD4`. Шина SmartWeb 20 кбит/с с активными устройствами. Драйвер
`drivers/net/can/ht42b416.c` (in-tree, `wirenboard/linux`, тег `v6.8.0-wb158`). Проверено на стенде,
в т.ч. сырым доступом к чипу по UART и логанализатором на физических линиях. Репро — в [`canMOD4-debug/`](canMOD4-debug/).

## Проблема
```
ht42b416 canMOD4: TX timeout waiting for Z
ht42b416 canMOD4: bus-off, scheduling restart in 1000 ms
ht42b416 canMOD4: Restart failed, error -ETIMEDOUT
```
После этого `canMOD4` висит в `NO-CARRIER`/`DOWN` до перезагрузки контроллера. Возникает за
секунды–минуты активной передачи.

## Подтверждённые факты

1. **«bus-off» синтетический — не настоящий CAN bus-off.** Это таймаут UART-ответа чипа на передачу,
   а не bus-off от CAN-контроллера. По исходнику (`drivers/net/can/ht42b416.c`, тег `v6.8.0-wb158`,
   сверено): `ht42b416_tx_timeout_work` по таймауту **500 мс** (`HT42B416_TX_RESP_TIMEOUT_MS`) ожидания
   ответа чипа — сперва `Z`-ack на команду передачи (стадия `TX_WAIT_Z`), затем статус `0x06`/`F06`
   (стадия `TX_WAIT_STATUS_RESP`) — зовёт `can_bus_off()` **без** инкремента счётчика (стр. 460–487).
   Настоящий bus-off идёт **другим** путём (`ht42b416_handle_status`, статус `STATUS_BUS_OFF`) и счётчик
   **инкрементит** `bus_off++` (стр. 550–554). На стенде/в поле срабатывает первый — отсюда наблюдаемая
   картина (`ip -details -statistics link show canMOD4`): счётчик `bus-off`=0, состояние `ERROR-ACTIVE`,
   TEC/REC чистые, `carrier`=0; мониторинг по одному `state` проблему не видит. В dmesg две формы:
   `waiting for Z` и `waiting for status 0x06` (стр. 482–483).

2. **Сбой на уровне модуля (чип/прошивка), не Linux-драйвера.** Симптом «нет `Z`» = чип перестал
   отвечать по UART на передачу; это немое состояние видно и по сырому UART с `mod4` в raw, **без**
   загруженного `ht42b416` (снято тестовой сессией: команды физически уходят на чип, ответа нет) — значит
   корень в самом модуле, а не в драйверной логике. **Условия возникновения НЕ локализованы:** стенд
   всегда с подключёнными устройствами SmartWeb, конфигурации «пустая шина» у нас нет, а CAN-шину мы не
   наблюдаем — поэтому связать залип с трафиком других устройств **нечем**. **Залип не зависит от
   содержимого передаваемых кадров:** произвольные кадры (случайные 29-битные ID, случайный DLC 0–8,
   мусорные данные — НЕ SmartWeb) вешают чип так же.

3. **UART-транспорт исправен — проблема не в нём.** Логанализатор на физических линиях (D1=TX, D0=RX) +
   счётчики порта показали: обмен хост↔провод совпадает байт-в-байт, хост ничего не теряет и не
   выдумывает. Перепроверено живьём 2026-06-25 на здоровом чипе: TX host↔провод **155/155** байт,
   RX **94/94** (с реальными `5A 0D`=`Z`, `46 06 0D`=`F`), serial-error дельта `oe/fe/pe/brk = 0`.

4. **Драйвер сдаётся после ОДНОЙ попытки.** `can_bus_off()` планирует рестарт через `restart-ms`
   (на стенде 1000 мс ставит `wb-hwconf-manager`); `can_restart` → `do_set_mode(START)` →
   `ht42b416_hw_start` (стр. 1188–1189) шлёт первым `RST` (стр. 326–331). Если чип ещё залип — `RST`
   не акается за 250 мс (`HT42B416_CMD_TIMEOUT_MS`, стр. 190–192) → `hw_start` возвращает `-ETIMEDOUT`;
   дальше mainline `can_restart` (can-dev) пишет `Restart failed` и **не перепланирует**. Итог —
   мёртвый интерфейс.

5. **Два класса залипа.**
   - **«Софт-восстановимый»** (чип жив): после остановки передач чип сам выходит из немоты — типично
     ~0.9 c, изредка до ~4.6 c (потолок не установлен).
     **Окно отсчитывается от ПОСЛЕДНЕЙ передачи, а не от начала залипа** — `T`-кадры
     в окне немоты отодвигают пробуждение:
     · на залипе **обычно** нем не только TX (`Z`), но и командный тракт — `RST`/`F` молчат и тогда оживают
       вместе с TX. Но **не всегда**: иногда командный тракт жив, `F`/`RST` отвечают, пока TX ещё нем —
       поэтому ответ `F` не доказывает готовность TX (см. факт 10);
     · для возврата TX **ни `RST`, ни реинит (`C`/`S`/`O`/фильтры) не нужны** — порт остаётся открыт через
       залип, конфиг цел; «вообще без команд» (только пауза) восстанавливают не хуже полного
       реинита; минимальный рецепт — **факт 10**;
     · «при частом TX `Z` не приходит НИКОГДА» — **не подтвердилось**: `T` каждые ~200 мс восстанавливаются
       за ~1–3 c.
   - **«Только power-cycle»** (чип нем): UART-TX чипа полностью мёртв — команды физически уходят, в ответ
     ничего. Программно/линейно не лечится (`RST`/`RCY`/`C`/`O`/DTR/RTS/BREAK —
     все немо). Единственное восстановление — power-cycle; на WB8.5 это `reboot` (= WBEC power-cycle,
     оживляет чип).

6. **Аппаратного сброса чипа из системы нет.** Драйвер поддерживает опциональный `enable-gpiod`
   (`devm_gpiod_get_optional(dev, "enable", …)`, стр. 1239): `hw_start` поднимает его в 1 (стр. 318),
   `hw_stop` опускает в 0 (стр. 293). Но пин `CE` чипа (`tCELOW=120 мкс`, даташит) в device-tree стенда
   **не разведён**, а питание слотов MOD неуправляемое. Причём даже будь `CE` разведён, авто-рестарт
   после bus-off его бы не дёрнул: путь `can_restart`→`hw_start` ставит enable=1 без фронта low→high
   (`hw_stop` в этом пути не зовётся) — фронт даёт лишь ручной `down`/`up` (stop→0, start→1). Для класса
   «только power-cycle» единственное восстановление — полный reboot.

7. **RX-парсер драйвера на десинке не клинит.** Неизвестный/битый старт-байт `ht42b416_rx_step`
   сбрасывает по одному (`rx_consume(…, 1)`, стр. 854–856; так же на `invalid DLC`, стр. 800–801),
   ресинхронизируясь побайтно ⇒ сам драйвер на рассинхроне UART-потока не залипает (виснет чип, не
   парсер).

8. **TEC/REC всегда 0 — неинформативны.** Драйвер не реализует `do_get_berr_counter`, поэтому счётчики
   ошибок CAN из чипа не читаются и `ip` всегда показывает 0 (это не «ошибок нет», а «их никто не
   опрашивает»). Диагностировать по TEC/REC нельзя.

9. **На 500 кбит/с залип не воспроизводится — но это не чистый вывод.** Проверено 2026-06-25
   на чистом чипе (после reboot, mod4 в raw — драйвер чип не настраивает); битрейт выставлен один раз на
   500 к (`S` code 7); app-like поллинг **2400 запросов за 20 мин — все получили `Z`, 0 залипов** (на
   20 к тот же поллинг виснет за ~15 с / ~#31). **Оговорка:** на 500 к чип не видит 20-кбит/с трафик
   SmartWeb — для него шина пустая (`0 incoming frames`, `Z` идёт, ответов нет). ⇒ показано лишь, что
   **на пустой (для чипа) шине он не виснет за 20 мин**; высокий битрейт против отсутствия встречного
   трафика тест **НЕ разделяет** (нужен реальный пир на 500 к, которого на стенде нет).

10. **Минимальное софт-восстановление: без `RST`, без посылок в шину; готовность TX — по ВРЕМЕНИ.**
    Для возврата TX ни `RST`, ни реинит (`C`/`S`/`O`/фильтры) не нужны — порт остаётся открыт через залип,
    конфиг цел; достаточно перестать передавать и дождаться самозаживления.
    - **TX-тракт оживает ~1.2 c+ от ПОСЛЕДНЕЙ передачи.** Ждали 1200 мс → TX готов. Точный потолок не установлен.
    - **`F` (статус) НЕ годится как детектор готовности TX.** `F` отвечает по UART, в шину ничего не шлёт,
      но **может отвечать, пока TX ещё нем** Если командный тракт на залипе не немеет, ответ `F` про
      готовность TX не говорит ничего. **Значение байта `F` тоже не спасает (проверено):** в момент ложной
      готовности `F` = `0xf0` (как в покое) или `0x06` (как после TX) — те же обычные коды, особого
      «TX-не-готов»/bus-off статуса нет. ⇒ надёжного непередающего сигнала готовности TX **не существует**,
      только время.
    - **`F` надёжен как детектор deep-dead:** молчит много секунд (>6 c) ⇒ «только power-cycle» (2 набл.).
    Алгоритм:
    ```
    залип (реальный запрос T не получил Z):
      1. перестать передавать;
      2. ждать ~1.2–1.5 c тишины (самозаживление TX от ПОСЛЕДНЕЙ передачи;
         ответ F раньше этого — НЕ доказательство готовности TX);
      3. возобновить обычный опрос (следующий реальный запрос = проба, лишнего в шину нет);
      4. снова залип → бэкофф подольше; после K неудач → deep-dead → reboot.
      (F-опрос опционально — раньше распознать deep-dead: F молчит много секунд ⇒ reboot)
    ```
    Класс «только power-cycle» так не лечится.

## Неподтверждённые гипотезы (минимум)
- **H1 — гонка «передача ⊗ входящий CAN-кадр» в прошивке.** Возможный механизм, но **ничем не
  подтверждён**: что залип вообще связан с трафиком других устройств — не доказано (конфигурации
  «пустая шина» у нас нет), а CAN-шину мы не наблюдаем вообще (логанализатор на UART). *Закрыть:*
  проверить, виснет ли модуль без других устройств на шине, **и** поставить независимый CAN-наблюдатель
  (второй интерфейс на той же шине + `candump` с HW-таймстампами).

## Что ещё проверить
1. **Чисто проверить гипотезу про битрейт (см. факт 9).** Прогон на 500 к дал «нет залипа», но это
   confound: на 500 к шина для чипа пустая. Чтобы отделить битрейт от встречного трафика — нужен реальный
   CAN-пир на 500 к (на стенде такого нет).
2. **Доводят ли передачи чип до необратимого «только power-cycle».** Подтверждено, что передачи **держат**
   чип немым (восстановимо). Проверить
   контролируемо: долбить передачами reboot-свежий чип N прогонов, считать необратимые смерти vs контроль. Сюда же — связь с полевым «вечно мёртвым» интерфейсом (приложение
   поллит передачами ~500 мс).

## Репро (минимум)
[`canMOD4-debug/`](canMOD4-debug/): `raw-uart-enable.sh` (перевод mod4 в raw-UART через
`/etc/wb-hardware.conf`) → `canmod4_applike_repro.py` (мастер как `wb-mqtt-smartweb` по сырому UART,
ловит залип) → `raw-uart-restore.sh` (вернуть `canMOD4`). Воспроизводится на стенде с подключёнными
устройствами SmartWeb (конфигурации без них у нас нет). Стенд: `root@10.100.2.243`, пароль `wirenboard`.
